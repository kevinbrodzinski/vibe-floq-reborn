// Main use-sync-external-store shim
import { useSyncExternalStore } from 'react';

// Main hook export
export { useSyncExternalStore };

// Default export for compatibility
export default useSyncExternalStore;