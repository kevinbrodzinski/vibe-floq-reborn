// src/lib/tokens/brand.ts
export const brand = {
  primary: 'hsl(var(--primary))',
  primaryDark: 'hsl(var(--primary-foreground))',
  accent: 'hsl(var(--accent))',
  danger: 'hsl(var(--destructive))',
  success: 'hsl(var(--success))',
} as const;